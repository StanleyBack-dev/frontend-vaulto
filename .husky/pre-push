#!/bin/sh

echo "[pre-push] Running prettier and lint checks..."
npm run prepush:check
status=$?

if [ "$status" -ne 0 ]; then
  echo "[pre-push] Push blocked. Fix local errors and warnings and try again."
  exit "$status"
fi

echo "[pre-push] Checks passed."