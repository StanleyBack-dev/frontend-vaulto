// Mirrors ACCOUNT_DELETION_GRACE_PERIOD_DAYS on the backend. Used here only
// for display copy (the actual deadline is computed and enforced server-side).
export const ACCOUNT_DELETION_GRACE_PERIOD_DAYS = 30;
